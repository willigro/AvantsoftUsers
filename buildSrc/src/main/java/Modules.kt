object Modules {

    const val app = ":app"
    const val datasource = ":datasource"
    const val common = ":common"
    const val components = ":components"

    val modules = listOf(
        app,
        datasource,
        common,
        components,
    )
}