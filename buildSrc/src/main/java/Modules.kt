object Modules {

    const val app = ":app"
    const val datasource = ":datasource"
    const val common = ":common"

    val modules = listOf(
        app,
        datasource,
        common,
    )
}